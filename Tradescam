-- VM.wtf PURE TRADE SCAMMER V3.0 - MINIMAL + ANTIBAN
-- Authorized Pentest Tool - Trade Scam Only
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- MINIMAL Config - Trade Scam ONLY
getgenv().PureTradeScam = {
    Enabled = false,
    Antiban = true
}

-- CRITICAL ANTIBAN (Essential)
local mt = getrawmetatable(game)
setreadonly(mt, false)

local oldNamecall = mt.__namecall
mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    -- BLOCK ALL KICKS
    if method:lower():find("kick") then
        return
    end
    
    -- BYPASS TRADE DETECTION
    if self:IsA("RemoteEvent") and getgenv().PureTradeScam.Antiban then
        local name = self.Name:lower()
        if name:find("trade") or name:find("anticheat") then
            -- Allow trade remotes, block anticheat
            if not name:find("accept") and not name:find("confirm") then
                return
            end
        end
    end
    
    return oldNamecall(self, ...)
end)

setreadonly(mt, true)

-- Normalize player stats (Anti-detection)
spawn(function()
    while true do
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            local hum = player.Character.Humanoid
            hum.WalkSpeed = 16
            hum.JumpPower = 50
            hum.JumpHeight = 7.2
        end
        task.wait(0.1)
    end
end)

-- PURE PINK GUI - Trade Scam Only
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PureTradeScam"
ScreenGui.Parent = playerGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 20, 147)
MainFrame.Position = UDim2.new(0.4, 0, 0.3, 0)
MainFrame.Size = UDim2.new(0, 350, 0, 280)
MainFrame.Active = true
MainFrame.Draggable = true

-- Pink Gradient
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 16)
Corner.Parent = MainFrame

local Gradient = Instance.new("UIGradient")
Gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 20, 147)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(199, 21, 133))
}
Gradient.Rotation = 45
Gradient.Parent = MainFrame

-- Title
local TitleBar = Instance.new("Frame")
TitleBar.Parent = MainFrame
TitleBar.BackgroundColor3 = Color3.fromRGB(199, 21, 133)
TitleBar.Size = UDim2.new(1, 0, 0, 50)

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 16)
TitleCorner.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Parent = TitleBar
TitleLabel.BackgroundTransparency = 1
TitleLabel.Size = UDim2.new(1, -80, 1, 0)
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "💖 VM.wtf PURE TRADE SCAM 💖"
TitleLabel.TextColor3 = Color3.new(1,1,1)
TitleLabel.TextScaled = true
TitleLabel.TextStrokeTransparency = 0

-- Close
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TitleBar
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 80, 120)
CloseBtn.Position = UDim2.new(1, -45, 0.15, 0)
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.new(1,1,1)
CloseBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)

-- Status
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Parent = MainFrame
StatusLabel.BackgroundColor3 = Color3.fromRGB(50, 205, 50)
StatusLabel.Position = UDim2.new(0, 10, 0, 65)
StatusLabel.Size = UDim2.new(1, -20, 0, 40)
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.Text = "🛡️ ANTIBAN ACTIVE - TRADE SCAM READY 🛡️"
StatusLabel.TextColor3 = Color3.new(1,1,1)
StatusLabel.TextScaled = true
Instance.new("UICorner", StatusLabel).CornerRadius = UDim.new(0, 10)

-- MAIN TOGGLE BUTTON
local ScamToggle = Instance.new("TextButton")
ScamToggle.Parent = MainFrame
ScamToggle.BackgroundColor3 = Color3.fromRGB(255, 150, 180)
ScamToggle.Position = UDim2.new(0.15, 0, 0.45, 0)
ScamToggle.Size = UDim2.new(0.7, 0, 0, 80)
ScamToggle.Font = Enum.Font.GothamBold
ScamToggle.Text = "🚫 TRADE SCAM OFF 🚫"
ScamToggle.TextColor3 = Color3.new(1,1,1)
ScamToggle.TextScaled = true

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 20)
ToggleCorner.Parent = ScamToggle

-- TRADE SCAM LOGIC
ScamToggle.MouseButton1Click:Connect(function()
    getgenv().PureTradeScam.Enabled = not getgenv().PureTradeScam.Enabled
    
    if getgenv().PureTradeScam.Enabled then
        ScamToggle.BackgroundColor3 = Color3.fromRGB(255, 50, 100)
        ScamToggle.Text = "💎 TRADE SCAM ON 💎"
        StatusLabel.Text = "💎 AUTO-ACCEPTING ALL TRADES - ACTIVE 💎"
        
        -- MAIN AUTO-ACCEPT LOOP
        spawn(function()
            while getgenv().PureTradeScam.Enabled do
                -- Scan GUI buttons (Humanized)
                for _, gui in pairs(playerGui:GetDescendants()) do
                    if gui:IsA("TextButton") or gui:IsA("ImageButton") then
                        local text = gui.Text:lower()
                        if text:find("accept") or text:find("confirm") or text:find("yes") or text:find("trade") then
                            task.wait(math.random(80, 200)/1000) -- Perfect human delay
                            pcall(function()
                                gui:Activate()
                                gui.MouseButton1Click:Fire()
                            end)
                            StatusLabel.Text = "💎 AUTO-ACCEPTED TRADE!"
                            break
                        end
                    end
                end
                task.wait(math.random(100, 400)/1000)
            end
        end)
        
    else
        ScamToggle.BackgroundColor3 = Color3.fromRGB(255, 150, 180)
        ScamToggle.Text = "🚫 TRADE SCAM OFF 🚫"
        StatusLabel.Text = "🛡️ ANTIBAN ACTIVE - READY 🛡️"
    end
end)

-- Trade Request Monitor
Players.PlayerAdded:Connect(function(plr)
    if plr ~= player and getgenv().PureTradeScam.Enabled then
        task.spawn(function()
            task.wait(math.random(500, 1500)/1000)
            StatusLabel.Text = "💎 TRADE FROM: " .. plr.Name
        end)
    end
end)

-- Remote Auto-Fire (Safe)
spawn(function()
    while true do
        if getgenv().PureTradeScam.Enabled then
            for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
                if remote:IsA("RemoteEvent") and remote.Name:lower():find("trade") then
                    pcall(function()
                        remote:FireServer("Accept", "Confirm")
                    end)
                end
            end
        end
        task.wait(math.random(2, 5))
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Final Status
StatusLabel.Text = "🛡️ PURE TRADE SCAM READY - CLICK TO ACTIVATE 🛡️"
print("💖 VM.wtf PURE TRADE SCAMMER V3.0 LOADED - AUTHORIZED PENTEST 💖")
print("🛡️ Full antiban active - Minimal design - Trade scam only!")
